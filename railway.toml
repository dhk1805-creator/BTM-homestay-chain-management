[build]
buildCommand = "cd /app && npx prisma generate --schema=packages/db/prisma/schema.prisma && cd packages/api && npx nest build"

[deploy]
startCommand = "node packages/api/dist/main"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
