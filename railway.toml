[build]
buildCommand = "cd /app && npx prisma generate --schema=packages/db/prisma/schema.prisma && npx nest build"

[deploy]
startCommand = "node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
