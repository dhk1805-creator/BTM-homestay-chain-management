[build]
buildCommand = "npx prisma generate --schema=../db/prisma/schema.prisma && npx nest build"

[deploy]
startCommand = "node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
